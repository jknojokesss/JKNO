{
  "name": "weldon-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Nightly Playwright scraper that pulls new Weldon orders into Supabase. Runs in GitHub Actions, kept separate from the Next app so it doesn't bloat the Vercel build.",
  "scripts": {
    "sync": "node sync.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "playwright": "^1.45.0",
    "ws": "^8.18.0"
  }
}
